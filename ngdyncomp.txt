angular dynamic components 
component = class + template
viewContainer <- view <- component <- componentFactory <- componentFactoryResolver
view can be an embeddedView (view:template) or HostView (view:component)
