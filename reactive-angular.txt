user input 					- capture user input with observable
 - keyboard
 - mouse
 - touch
 - web device

browser
 - requestAnimationFrame
 - scroll

angular
  - forms , form control, reactive forms 	- capture form input with observable
  - router					- capture url change with observable
  - http request				- capture http request with observable
  - web socket request,                         - capture web socket request with observable
  - @Input, @Output -> ngOnChanges		- capture component input(select) / output(action) with observable
  - query viewchildren list
  - 

Requirements:
--------------------------------------------------------------------------
1 - all angular component communication with rxjs observables