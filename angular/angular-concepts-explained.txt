Directives
--------------------------------------
At the core, a directive 
- is a function that executes whenever the Angular compiler finds it in the DOM. 
- used to extend the power of the HTML by giving it new syntax. 

 Each directive has a name — either one from the Angular 
 - predefined like ng-repeat , 
 - or a custom one which can be called anything